apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.springboot.name }}
spec:
  replicas: {{ .Values.springboot.replica }}
  selector:
    matchLabels:
      app: {{ .Values.springboot.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.springboot.name }}
    spec:
      initContainers:
        
        - name: wait-for-mysql
          image: busybox
          command: ['sh', '-c', 'until nc -z mysql 3306; do echo waiting for mysql; sleep 2; done;']
        
        - name: wait-for-rabbitmq
          image: busybox
          command: ['sh', '-c', 'until nc -z rabbitmq 5672; do echo waiting for rabbitmq; sleep 2; done;']
        
        - name: wait-for-redis
          image: busybox
          command: ['sh', '-c', 'until nc -z redis 6379; do echo waiting for redis; sleep 2; done;']
      
      containers:
        - name: springboot
          image: "{{ .Value.springboot.image.repository }}:{{ .Values.springboot.image.tag }}"
          ports:
            - containerPort: {{ .Values.springboot.port }}
          env:
            - name: SPRING_DATASOURCE_URL
              value: "{{ .Values.springboot.datasource.url }}"
            - name: SPRING_DATASOURCE_USERNAME
              value: "{{ .Values.springboot.datasource.username }}"
            - name: SPRING_DATASOURCE_PASSWORD
              value: "{{ .Values.springboot.datasource.password }}"